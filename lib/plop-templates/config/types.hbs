import type { ComponentProps } from 'svelte';
import type { EmbedWindow } from 'svelte-standalone';

import {{capitalizeName}} from './index.svelte';

export type {{capitalizeName}}Props = ComponentProps<{{capitalizeName}}>;

export const defaultConfig: {{capitalizeName}}Props = {};

declare global {
	interface Window extends EmbedWindow<typeof {{capitalizeName}}, '{{componentName}}'> {}
}