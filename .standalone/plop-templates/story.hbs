import type { Meta, StoryObj } from '@storybook/svelte';

import {{capitalizeName}} from '@/_widgets/{{componentName}}/index.svelte';
import { defaultConfig } from '@/_widgets/{{componentName}}/types';

export default {
	title: 'Widgets/{{capitalizeName}}',
	component: {{capitalizeName}}
} satisfies Meta<{{capitalizeName}}>;

export const {{componentName}}Story: StoryObj<{{capitalizeName}}> = {
	args: defaultConfig
};
