import type { Meta, StoryObj } from '@storybook/svelte';

import {{capitalizeName}} from '@/_standalone/{{componentName}}/index.svelte';

export default {
	title: 'Standalone/{{capitalizeName}}',
	component: {{capitalizeName}}
} satisfies Meta<{{capitalizeName}}>;

export const {{capitalizeName}}Story: StoryObj<{{capitalizeName}}> = {
	args: {} // don't have args since auto-embeddable
};
