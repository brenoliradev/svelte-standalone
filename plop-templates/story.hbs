import type { SvelteComponent } from 'svelte';

import type { Meta, StoryObj } from '@storybook/svelte';

import { defaultConfig, type ConfigProps } from '@/_widgets/{{componentName}}/types';

import {{capitalizeName}} from '@/_widgets/{{componentName}}/index.svelte'

export default {
	title: 'Widgets/{{capitalizeName}}',
	component: {{capitalizeName}},
} satisfies Meta<SvelteComponent<ConfigProps>>;

export const {{componentName}}Story: StoryObj<SvelteComponent<ConfigProps>> = {
	args: defaultConfig
}