import type { {{capitalizeName}}Props } from "./declarations";

export const defaultConfig: {{capitalizeName}}Props = {};

export interface CustomWindow extends Window {
	{{componentName}}Start(config: {{capitalizeName}}Props): void;
	{{componentName}}Stop(): void;
}