{{!-- TYPES SHOULD BE ADDED ON TYPES.TS TO EASILY USE IT SEE: https://github.com/sveltejs/svelte/issues/5817 --}}
export type ConfigProps = undefined;

export const defaultConfig: ConfigProps = undefined;

export type CustomWindow = Window & {
	{{componentName}}Start: (config: ConfigProps) => void;
	{{componentName}}Stop: () => void;
};